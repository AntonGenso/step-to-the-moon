services:
  frontend:
    container_name: sttm_frontend_prod
    # GITHUB_REPOSITORY=owner/repo, e.g. alfat/step-to-the-moon
    image: ghcr.io/${GITHUB_REPOSITORY}:latest
    restart: unless-stopped
    ports:
      - "3000:3000"
    networks:
      - prod

networks:
  prod:
